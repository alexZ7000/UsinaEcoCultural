import getDocumentElement from "@popperjs/core/dist/esm/dom-utils/getDocumentElement.js";
import getComputedStyle from "@popperjs/core/dist/esm/dom-utils/getComputedStyle.js";
import getWindowScrollBarX from "@popperjs/core/dist/esm/dom-utils/getWindowScrollBarX.js";
import getWindowScroll from "@popperjs/core/dist/esm/dom-utils/getWindowScroll.js";
import { max } from "@popperjs/core/dist/esm/utils/math.js"; // Gets the entire size of the scrollable document area, even extending outside
// of the `<html>` and `<body>` rect bounds if horizontally scrollable

export default function getDocumentRect(element) {
  var _element$ownerDocumen;

  var html = getDocumentElement(element);
  var winScroll = getWindowScroll(element);
  var body = (_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body;
  var width = max(html.scrollWidth, html.clientWidth, body ? body.scrollWidth : 0, body ? body.clientWidth : 0);
  var height = max(html.scrollHeight, html.clientHeight, body ? body.scrollHeight : 0, body ? body.clientHeight : 0);
  var x = -winScroll.scrollLeft + getWindowScrollBarX(element);
  var y = -winScroll.scrollTop;

  if (getComputedStyle(body || html).direction === 'rtl') {
    x += max(html.clientWidth, body ? body.clientWidth : 0) - width;
  }

  return {
    width: width,
    height: height,
    x: x,
    y: y
  };
}