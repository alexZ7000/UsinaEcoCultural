import getWindowScroll from "@popperjs/core/dist/esm/dom-utils/getWindowScroll.js";
import getWindow from "@popperjs/core/dist/esm/dom-utils/getWindow.js";
import { isHTMLElement } from "@popperjs/core/dist/esm/dom-utils/instanceOf.js";
import getHTMLElementScroll from "@popperjs/core/dist/esm/dom-utils/getHTMLElementScroll.js";
export default function getNodeScroll(node) {
  if (node === getWindow(node) || !isHTMLElement(node)) {
    return getWindowScroll(node);
  } else {
    return getHTMLElementScroll(node);
  }
}