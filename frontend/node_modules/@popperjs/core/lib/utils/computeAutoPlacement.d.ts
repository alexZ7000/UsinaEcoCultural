import type { State, Padding } from "@popperjs/core/lib/types";
import type { Placement, ComputedPlacement, Boundary, RootBoundary } from "@popperjs/core/lib/enums";
declare type Options = {
    placement: Placement;
    padding: Padding;
    boundary: Boundary;
    rootBoundary: RootBoundary;
    flipVariations: boolean;
    allowedAutoPlacements?: Array<Placement>;
};
export default function computeAutoPlacement(state: Partial<State>, options?: Options): Array<ComputedPlacement>;
export {};
