import getWindowScroll from "@popperjs/core/lib/dom-utils/getWindowScroll.js";
import getWindow from "@popperjs/core/lib/dom-utils/getWindow.js";
import { isHTMLElement } from "@popperjs/core/lib/dom-utils/instanceOf.js";
import getHTMLElementScroll from "@popperjs/core/lib/dom-utils/getHTMLElementScroll.js";
export default function getNodeScroll(node) {
  if (node === getWindow(node) || !isHTMLElement(node)) {
    return getWindowScroll(node);
  } else {
    return getHTMLElementScroll(node);
  }
}